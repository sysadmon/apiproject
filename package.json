{
  "name": "apiproject",
  "version": "1.0.0",
  "description": "API test suite for testimonials, help center, agents, newsletter and optin",
  "main": "index.js",
  "scripts": {
    "test": "mocha \"tests/**/*.test.js\" --timeout 30000 --reporter spec",
    "test:ci": "mocha \"tests/**/*.test.js\" --timeout 30000 --reporter spec"
  },
  "keywords": [
    "api",
    "testing",
    "mocha",
    "chai"
  ],
  "author": "sysadmon",
  "license": "MIT",
  "devDependencies": {
    "@faker-js/faker": "^9.0.0",
    "chai": "^4.5.0",
    "mocha": "^10.7.3"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
