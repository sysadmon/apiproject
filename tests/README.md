# Zedu Chat API Automation

API automation test suite for `https://api.staging.zedu.chat/api/v1/` using Node.js, Jest, and Supertest.

## Prerequisites
- Node.js >= 18.0.0
- npm >= 9.0.0

## Setup

1. Clone the repository
git clone <repo-url>
cd apiproject